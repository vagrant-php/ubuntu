- name: copy .application_aliases for user vagrant
  copy:
    src=symfony3
    dest=/home/vagrant/.application_aliases/symfony3
    owner=vagrant
    group=vagrant

- name: copy .application_aliases for user root
  copy:
    src=symfony3
    dest=/root/.application_aliases/symfony3
    owner=root
    group=root

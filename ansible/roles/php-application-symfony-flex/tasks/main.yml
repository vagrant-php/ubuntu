- name: copy .application_aliases for user vagrant
  copy:
    src=symfony-flex
    dest=/home/vagrant/.application_aliases/symfony-flex
    owner=vagrant
    group=vagrant

- name: copy .application_aliases for user root
  copy:
    src=symfony-flex
    dest=/root/.application_aliases/symfony-flex
    owner=root
    group=root
